import 'package:flutter/material.dart';
import 'package:roll_dise/gradient_container.dart';

void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      // backgroundColor: Colors.orange,
      body: GradientContainer(), //  here we use our custome widget
    ),
  ));
}
